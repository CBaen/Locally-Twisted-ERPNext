# Migration Priorities - Retired Prior-Host Runbook

This April 2026 gate-kit planning runbook has been retired as active guidance.
It contained prior-host and Claude-era setup paths that are no longer valid on
the current Wardenclyffe Kubuntu checkout.

Use these current sources instead:

- `AGENTS.md` for repo rules, current startup order, and Kubuntu paths.
- `CODING-HANDOFF.md` for compact current technical state.
- `capabilities/INDEX.md` plus task-specific capability recipes for operating
  rules.
- `scripts/verify/kubuntu_doctor.py` and `verifier-manifest.json` for current
  Kubuntu verification surfaces.

Historical details remain available through git history if needed, but they are
not current instructions and should not be copied forward.
