# ChatGPT / Codex Start Here

Read `AGENTS.md` first, then `CODING-HANDOFF.md`.

This repo is intentionally moving away from Claude-specific handoff files. `CLAUDE.md`, `HANDOFF.md`, and `PROJECT-STATUS.md` may contain useful history, but they also contain stale counts and tool-specific framing. Verify before relying on them.

Core rule: never tell GL something is true unless it has been checked against files, git, the running ERPNext database, or the browser.
