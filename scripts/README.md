# Locally Twisted - scripts/

Operational scripts for the LT ERPNext build live here. Most scripts are self-contained Python files with a docstring at the top explaining purpose, usage, and why it exists. The layout-fit gate is a Playwright Test spec and runs through npm.

Run scripts from the project root: `python scripts/<dir>/<name>.py`.
Run the layout-fit gate from the project root: `npm run test:layout-fit`.
Run the public axe accessibility gate from the project root: `npm run test:a11y`.
Run the verifier CLI contract from the project root: `python scripts/verify/verifier_cli_contract.py`.

Verification scripts must treat `--help` as a safe, fast, non-mutating command.
If a verifier needs a browser, Docker, ERPNext, fake-data writes, or network
access for its normal run, its help path must exit before any of that work
starts. The `verifier_cli_contract.py` gate enforces this for tracked,
maintained Python verifiers. Throwaway `_oneshot_*` scripts are excluded and
should not be used as launch proof.

## Layout

| Dir | Purpose |
|-----|---------|
| `setup/` | One-time-or-occasional install/configuration scripts. Idempotent scripts are safe to re-run. |
| `dev/` | Day-to-day development helpers. Run during a build session. |
| `fix/` | Patches that work around upstream bugs or recreate transient state. |
| `translate/` | Historical translation scripts were removed; git history is the archive. |
| `verify/` | Verification scripts. Run before declaring anything done. |

## setup/

| Script | Purpose | Run when |
|--------|---------|----------|
| `setup_lt_company.py` | One-shot wizard completion + LT Company seeding. | Once, on a fresh install |
| `setup_slice2_header_footer.py` | Stale Slice 2 Website Settings wiring attempt. | Do not re-run without reading the current header/footer rules |
| `install_webshop.py` | Historical/fallback installer for `frappe/webshop` + `frappe/payments`. | Only when deliberately rebuilding a bind-mount install path |
| `export_odoo_catalog.py` | One-shot HTML scraper for the old live LT Odoo catalog. | Re-run only before the old source is decommissioned |
| `sync_contact_intake_backend.py` | Runs the in-app `locally_twisted.seed.sync_contact_intake_backend.execute` sync so ERPNext Lead/CRM metadata matches the current `/contact` service taxonomy, including plain-text estimated time fields and the `LT Lead Photo` child table connection. | After changing public contact service labels or backend Lead conditional logic |
| `sync_permission_hardening.py` | Runs the in-app `locally_twisted.seed.sync_permission_hardening.execute` sync so LT-owned custom/admin DocTypes do not retain broad `All` mutation permissions. | After changing LT custom DocType permissions or before human-access rollout |
| `sync_crm_pipeline.py` | Runs the in-app `locally_twisted.seed.sync_crm_pipeline.execute` sync so LT's six-stage CRM board uses `Lead.custom_pipeline_stage` instead of repurposing ERPNext's native `Lead.status`. | After changing CRM stages, the LT Inquiry Board, or Owner Home inquiry-count behavior |
| `sync_stage_cascade.py` | Runs the in-app `locally_twisted.seed.sync_stage_cascade.execute` sync so Task has the Lead/stage fields required by safe CRM stage cascades. `sync_crm_pipeline.py` also runs this sync. | After changing CRM stage-to-Task cascade fields |
| `sync_backend_workspaces.py` | Runs the in-app `locally_twisted.seed.sync_backend_workspaces.execute` sync so Owner, Manager, and Employee workspaces use current business labels, the Sales Order booking calendar, Owner Home command-center cards/chart/checklist, and deterministic persona shortcut sets. | After changing simplified backend workspaces, role profiles, number cards, charts, or calendar shortcuts |
| `sync_owner_demo_data.py` | Seeds or removes clearly marked fake owner-action Leads, a Customer/Contact, and a draft Sales Order so `/owner-actions` and owner Desk demos show useful local records without real customers or live sends. | Before local owner access demos, `npm run test:owner-actions`, or cleanup with `--cleanup` when the fake tour records are no longer wanted |
| `sync_finance_workspace.py` | Runs the in-app `locally_twisted.seed.sync_finance_workspace.execute` sync so Accountant Home has finance cards, accounting shortcuts, the internal customer reminder review report shortcut, and no unfinished bank/vendor/payroll setup links. | After changing accountant workspace shortcuts, finance cards, report records, or reminder review report Desk wiring |
| `sync_customer_portal.py` | Runs the in-app `locally_twisted.seed.sync_customer_portal.execute` sync so the invite-only customer/client portal menu points at LT-owned `/account/*` and `/organization` routes, hides stock/native customer routes, keeps supplier routes Supplier-only, and preserves guest inquiry/cart/checkout boundaries. | After changing Portal Settings, customer account menu labels, signup policy, or customer/client portal route visibility |
| `sync_marketing_review_access.py` | Runs the in-app `locally_twisted.seed.sync_marketing_review_access.execute` sync so the no-Desk external marketing review role exists, has no DocPerm rows, and passes the marketing boundary check. | After changing marketing reviewer access, external review routes, or human-access rollout gates |
| `sync_site_branding.py` | Runs the in-app `locally_twisted.seed.sync_site_branding.execute` sync so public/login favicon, logo, splash, app name, and source-level head chrome stay Locally Twisted branded. | After changing public/login branding, favicon assets, or rebuilding Website/System Settings |
| `sync_invoice_branding.py` | Runs the in-app `locally_twisted.seed.sync_invoice_branding.execute` sync so Sales Invoices use the branded Locally Twisted print format, default print-format property, and letterhead. | After changing invoice print copy, invoice styling, letterhead, or rebuilding setup records |
| `sync_maintenance_package.py` | Runs the in-app `locally_twisted.seed.sync_maintenance_package.execute` sync so the sanitized Maintenance Admin role, heartbeat report, workspace shortcuts, and read-only DocPerm boundary are present. | After changing maintenance heartbeat DocTypes, report wiring, workspace shortcuts, or Maintenance Admin access rules |
| `sync_variant_media.py` | Stages Odoo product images and applies conservative variant image mappings. | After reviewing or refreshing catalog media mappings |
| `sync_category_media.py` | Writes an approval template from category candidates, then stages and dry-runs or applies approved Item Group image selections through Frappe. `--apply` refuses unapproved selections. | After Jeff/GL approve category browse images |

## dev/

| Script | Purpose | Run when |
|--------|---------|----------|
| `clear_website_cache.py` | Clears Frappe site + website cache so edited Jinja templates / CSS / Web Page records take effect on the next request. Optional `--restart` for `hooks.py` changes. | After editing Jinja templates, SCSS/CSS, Web Page records, or hooks |

## fix/

| Script | Purpose | Run when |
|--------|---------|----------|
| `patch_nginx_socketio_origin.py` | Historical/fallback patch for the LT frontend container's nginx config to pass through the original `Origin` header. The current custom image should already contain this line. | Only if a rebuilt frontend image is verified missing `proxy_set_header Origin $http_origin;` |
| `fix_crm_lead_*.py` | Removed. These one-off Lead-schema scripts used stale service labels and are preserved only in git history. | Use `setup/sync_contact_intake_backend.py` instead |
| `fix_lead_photo_thumbnail.py` | Removed. The child table connection is now handled by `setup/sync_contact_intake_backend.py`; thumbnail UX remains a separate product choice. | Use git history only if researching the old experiment |

## translate/

| Script | Purpose | Status |
|--------|---------|--------|
| `translate_crm_lead.py` | Removed. It built an early Lead schema with stale service values. | Git history only |
| `translate_dashboard_review.py` | Built the `Dashboard Reviewed Item` DocType. | Historical |

## verify/

| Script | Purpose | Run when |
|--------|---------|----------|
| `a11y_audit.js` | Playwright + axe-core gate for the public launch route list at desktop and mobile widths. Writes `output/a11y/a11y-desktop.json`, `output/a11y/a11y-mobile.json`, and `output/a11y/a11y-summary.json`; fails nonzero on any axe violation. | Before closing public route/template changes, especially landmarks, headings, links, forms, breadcrumbs, checkout, shop, or customer-facing CSS |
| `layout_fit.spec.js` | Playwright Test gate for public/shop/cart routes across mobile, tablet, and desktop widths. | Before visual claims, after customer-facing CSS/Jinja/template changes |
| `search_contract.spec.js` | Playwright Test gate for header search overlay product suggestions and submitted `/shop?q=...` results. It distinguishes hidden filtered quick-link nodes from owner-excluded links that should be absent. | After changing header search, shop search, Website Item search context, Ready-to-Order quick links, or product-card discovery behavior |
| `portfolio_reel.spec.js` | Playwright Test gate for `/portfolio` floating proof-reel behavior: natural-ratio images, no photo captions/frame wrappers, no route-specific portfolio contact/index footer, larger desktop entry/click-to-front behavior that settles without pointer-follow sway, mobile full-width slide-in reveal, filter relayout, and empty state. | After editing portfolio layout, image metadata, filters, or modal behavior |
| `owner_desk_routes.spec.js` | Playwright Test gate for owner Desk route recovery and Owner Home content. | After Desk JS, workspace, or simplified owner role changes |
| `contact_service_logic.py` | Verifies `/contact` service-specific conditional logic and absence of stale service labels. | After editing contact form labels, choices, conditionals, or Lead payload mapping |
| `contact_prefill.py` | Verifies guided contact URLs preselect the intended service checkboxes and panels. | After editing service-page CTAs or `/contact?service=...` parsing |
| `lead_backend_intake_parity.py` | Verifies live ERPNext Lead/CRM metadata matches `/contact`: service type records, Lead Custom Field labels/depends_on logic, plain-text time entry, `LT Lead Photo` table wiring, and submit helper mapping into `custom_event_type`. | After editing backend Lead fields or public service taxonomy |
| `custom_doctype_permission_boundary.py` | Verifies LT-owned custom/admin DocTypes do not expose broad `All` mutation rights and enabled Website Users cannot mutate `LT Service Type`. | After changing LT custom DocType permissions or human-access rollout gates |
| `crm_pipeline_parity.py` | Verifies LT's Odoo-approved six-stage inquiry board is backed by `Lead.custom_pipeline_stage`, leaves native `Lead.status` intact, removes stale status columns from `LT Inquiry Board`, and ensures website Leads start at `New Inquiry`. | After editing CRM stage sync, inquiry Kanban behavior, or Owner Home inquiry filters |
| `crm_stage_cascade.py` | Verifies stage movement creates/closes only operational Tasks, leaves Sales Orders, Sales Invoices, and Payment Requests unchanged, and cleans up its temporary test records. | After editing `stage_cascade.py`, Lead hooks, or CRM stage-to-Task behavior |
| `backend_schema_inventory.py` | Read-only live ERPNext backend inventory: counts core records/schema records, classifies Custom Fields as code-owned vs unclassified DB/app-owned, maps current CRM/checkout cascade surfaces, and separates intentional old-label guardrails from stale references. | Before deciding backend fixture/export ownership, Lead layout simplification, or new stage-to-finance cascades |
| `backend_schema_inventory_contract.py` | Unit contract for `backend_schema_inventory.py` helper logic. | After editing the backend inventory classifier or stale-term scanner |
| `verifier_cli_contract.py` | Fast meta-contract proving tracked maintained Python verifiers expose safe `--help` output instead of launching browser/backend work. | After adding or changing Python verifier entrypoints, and before trusting a timeout as a product failure |
| `backend_workspace_parity.py` | Verifies simplified backend workspaces no longer show stale ERPNext labels, booking calendars point at Sales Orders by delivery date, Owner Home includes the command-center number cards/chart/checklist, and Manager/Employee workspaces do not regain non-persona shortcuts. | After editing workspaces, role profiles, number cards, charts, or Desk calendar behavior |
| `finance_workspace_parity.py` | Verifies Accountant Home cards, reminder-review report wiring, simplified accounting shortcuts, and the absence of unfinished bank/vendor/payroll setup links. | After editing Accountant Home, finance cards, report records, reminder-review wiring, or finance/payroll setup visibility |
| `persona_desk_routes.spec.js` | Browser login proof for Manager, Employee, and Accountant temp users. Verifies each persona lands on its personalized Desk workspace and does not see shortcuts intentionally removed from that role. Run through `npm run test:desk-personas` with `LT_DESK_TEST_PASSWORD`. | After changing Manager, Employee, Accountant, role profile, workspace, or Desk route behavior |
| `owner_action_center.spec.js` | Browser login proof for `/owner-actions`: owner sees the phone-first call/text page, urgent fake contacts, upcoming bookings, `tel:`/`sms:` links, and no platform jargon. Run through `npm run test:owner-actions` with `LT_DESK_TEST_USER` and `LT_DESK_TEST_PASSWORD`. | After changing owner phone access, owner demo records, owner DTOs, or the `/owner-actions` page |
| `owner_business_access_contract.py` | Rollback-safe live ERPNext contract for the provider-neutral owner business API. Seeds temporary fake records, proves `locallytwisted@gmail.com` can read owner-safe DTOs, proves Guest is blocked, proves the only write is `log_contact_attempt`, and proves that write creates one Comment and no Email Queue. | After changing owner business access APIs, assistant integration boundaries, owner contact logging, or provider/OAuth adapter planning |
| `customer_documents_contract.py` | Verifies policy lane anchors, code-owned customer email policy blocks, and required internal copy recipients for inquiry acknowledgments without creating ERPNext setup records. | After editing customer emails, receipts, checkout notices, copy routing, or policy pages |
| `customer_email_policy_contract.py` | Static no-send check for inquiry acknowledgment, receipt, operator, and welcome email policy lanes, queue behavior, and no-PDF/no-attachment boundaries. | After editing customer/operator email bodies, receipt handling, welcome copy, or payment cascade email tests |
| `customer_account_provisioning_contract.py` | Rollback-safe live ERPNext contract for invite-only customer account provisioning. Proves a Contact linked to a Customer can create/reuse a Customer Website User without sending email, and missing Customer/email/backend-user collision cases fail loudly. | After changing customer account provisioning, Contact/Customer/User linking, account invite assumptions, or portal signup boundaries |
| `customer_portal_inventory.py` | Read-only login/account portal inventory. Normal mode checks hard boundaries (`/login`, guest `/me`, signup disabled, guest ecommerce settings); `--strict-menu` additionally fails unless Portal Settings has the LT customer menu labels, hidden stock customer/public routes, and Supplier-only supplier routes. | After changing customer/client portal settings, signup/login policy, Portal Settings menu rows, or customer account route visibility |
| `customer_portal_home_contract.py` | Rollback-safe live ERPNext contract for `/me`. Creates a temporary Customer Website User, renders the account home as that user, proves LT account-home markers and customer sidebar routes, rejects native/internal ERPNext account text, and rolls back the fake user. | After changing `/me`, customer account-home copy/style, customer portal sidebar behavior, invite-only account assumptions, or portal route labels |
| `customer_portal_v1_contract.py` | Rollback-safe live ERPNext contract for the LT-owned customer portal V1. Creates temporary Customer/Contact/User/Sales Order/organization membership records, renders all individual and organization routes, proves all eight module keys exist, verifies change/repeat actions create review requests without mutating the Sales Order, and proves customer portal file registration requires same-user/same-source ownership. | After changing `customer_portal.py`, customer account routes, review-request actions, organization portal membership, customer file visibility, or customer-safe DTO fields |
| `marketing_review_access_boundary.py` | Rollback-safe live ERPNext contract for the external marketing review role. Creates a temporary marketing Website User, proves the role has no forbidden roles or backend record access, proves `/me` redirects to `/marketing-review`, proves customer users cannot reach the review route, and rolls back the fake user. Run through `npm run test:marketing-review-access`. | After changing marketing reviewer access, `/marketing-review`, `/me`, permission hooks, human-access rollout gates, or external reviewer provisioning |
| `maintenance_heartbeat.py` | Verifies the sanitized client operations heartbeat: public boot, scheduler, notification preferences, Maintenance Admin boundary, and optional heavy paperwork/business digest checks. | After changing maintenance heartbeat code, scheduler hooks, checkup topics, or owner digest boundaries |
| `maintenance_admin_boundary.py` | Verifies the Maintenance Admin role can read only sanitized maintenance DocTypes/report/workspace shortcuts and cannot read raw logs, customer records, files, communications, or finance records. | After changing maintenance role permissions, DocTypes, workspace shortcuts, or report roles |
| `paperwork_status.py` | Read-only paperwork/backend status report for invoices, payment requests, email queues, non-live setup gaps, and cutover-deferred payment items. It does not run live payment readiness in synthetic mode. Outputs optional JSON to ignored `output/`. | When reviewing back-office launch readiness before adding finance automation |
| `quote_proposal_draft_packet.py` | Draft-only quote/proposal packet renderer. Produces internal review packets from existing Quotation data while proving no PDF, customer send, or finance mutation happens. Product-page quotes still using the zero-dollar review line are labeled `Pricing review required` and blocked from send-readiness instead of being shown as reviewed `$0.00` customer totals. | Before building proposal PDF output, sender UX, quote approval automation, or any customer-facing proposal delivery |
| `quote_proposal_draft_packet_contract.py` | Fake-data contract for normal/outlier quote and proposal review packets, including missing acceptance path, quote-first placeholder pricing, reviewed product quote still draft-only behavior, and malformed send-ready source behavior. | After changing quote/proposal packet copy, packet shape, send-readiness rules, or review-gate boundaries |
| `product_quote_operator_review.py` | Read-only live product-page quote operator-review report. Lists product quote Quotations, blockers, and internal `Ready For Customer Review` state without sending, creating Sales Orders, creating invoices, creating Payment Requests, or enabling customer acceptance. | Before building customer quote sender, acceptance route, or accepted-quote-to-order workflow |
| `product_quote_operator_review_contract.py` | Fake-data contract for product quote operator-review behavior: placeholder zero-price lines block customer review, malformed payloads block review, and reviewed quotes can become internally ready while still forbidding send/order/payment. | After changing product quote review blockers, draft Quotation bridge behavior, or quote readiness rules |
| `product_quote_acceptance_contract.py` | Rollback-safe live ERPNext contract for accepted product-page quotes. Proves a submitted, priced, customer-accepted product quote creates only a draft Sales Order, stores source quote and written-approval details, preserves product-page line payloads, issues token acceptance links for `/quote-accept`, blocks expired links loudly, and does not create invoices, payment requests, Email Queue rows, or Communications. | After changing accepted product quote behavior, quote-to-order mapping, Sales Order acceptance fields, quote approval links, product quote blockers, or finance/email boundaries |
| `quote_accept_experience.spec.js` | Playwright customer-experience gate for `/quote-accept`. Proves a missing approval code fails loudly with a safe `/contact` fallback, the post-approval route template has a distinct no-payment success panel instead of leaving the customer in an editable form, and a real temporary token can preview a reviewed quote, submit guest approval, create one draft Sales Order, create no invoice/payment request, and clean up the fake records. Run with `npm run test:quote-accept-experience`. | After changing `/quote-accept`, quote approval page copy, acceptance form behavior, success/failure states, token acceptance behavior, or quote approval customer UX |
| `product_quote_customer_delivery_contract.py` | Rollback-safe live ERPNext contract for sending reviewed product-page quote approval links. Stubs `frappe.sendmail`, requires the customer recipient and delivery-safe business BCC, blocks routed-alias BCCs such as `hi@locallytwisted.com`, checks the email includes `/quote-accept`, and proves no Sales Order, Sales Invoice, Payment Request, Email Queue, or Communication records change during the test. | After changing product quote customer email delivery, BCC policy, approval-link email copy, or quote sender boundaries |
| `product_quote_operator_send_control_contract.py` | Rollback-safe live ERPNext contract for the operator-owned Quotation send control. Proves the Desk button hook exists, the whitelisted method is not guest-callable, only quotes marked `Ready For Customer Review` can send, the customer email includes `/quote-accept`, the delivery-safe business mailbox is BCC'd, and no order/finance/email records change. | After changing Quotation Desk controls, product quote approval-link sending, quote readiness blockers, BCC routing, or operator customer-delivery gates |
| `product_quote_customization_contract.py` | Rollback-safe live ERPNext contract for quote-first customization payloads. Proves balloon color choices become structured `color_recipes` with grouped color metadata, survive the Lead-to-draft-Quotation bridge, and malformed color payloads fail loudly. | After changing quote-first customization payloads, color drawer behavior, color recipe normalization, Lead quote handoff, or draft Quotation payload preservation |
| `unpaid_invoice_review.py` | Draft-only unpaid/overdue invoice review surface. Produces reminder and statement candidates from Sales Invoices plus the outbound document registry while proving no customer send or accounting mutation happens. | Before creating payment reminders, statement drafts, or any collections/reconciliation review queue |
| `unpaid_invoice_draft_packet.py` | Draft-only unpaid invoice packet renderer. Produces JSON and optional Markdown review packets from unpaid invoice candidates while proving no customer send or accounting mutation happens. | Before building an internal Desk queue, scheduled digest, or any reminder/statement send path |
| `unpaid_invoice_draft_packet_contract.py` | Fake-data contract for normal/outlier unpaid invoice draft packet behavior, including missing payment requests and malformed human-approval gates. | After changing unpaid invoice packet copy, packet shape, or review-gate rules |
| `paperwork_review_digest.py` | Read-only internal digest combining paperwork status, automation index, unpaid invoice review, draft packets, and audience-specific operations readiness rows into one review payload. It verifies the digest uses the automation index without runtime fake-data contracts. | Before creating a Desk queue or scheduled internal digest and after changing paperwork review surfaces |
| `customer_reminder_dry_run.py` | No-live internal customer reminder review queue. Builds cadence suggestions, blockers, and draft sections without enabling customer delivery. | Before any reminder UX, scheduled internal report, or future customer-send approval work |
| `customer_reminder_dry_run_contract.py` | Fake-data contract for no-live customer reminder queue behavior, including overdue/current/missing-payment-path/malformed-send scenarios. | After changing reminder cadence rules, blockers, queue shape, or dry-run boundaries |
| `customer_reminder_review_report.py` | No-live customer reminder report data source. Turns dry-run queue items into table rows, review/hold groups, optional JSON/Markdown/CSV output, and the internal Desk report path without customer delivery. | Before changing the Desk report, internal report, or scheduled internal-only reminder review surface |
| `customer_reminder_review_report_contract.py` | Fake-data contract for customer reminder report rows/groups, including empty queues and malformed send-enabled source rows. | After changing reminder report columns, groups, or no-live row boundaries |
| `record_level_failure_contract.py` | Rollback-safe fake-data contract proving backend partial failures create record-level Comments plus Error Log calls through the reusable failure recorder, and that exact resolution comments close a grouping key without deleting the original evidence. | After changing fail-loud recorder behavior, Lead cascade failure handling, checkout partial-failure handling, paid-order receipt handling, failure-resolution handling, or automation-index record health rows |
| `inquiry_upload_failure_contract.py` | Rollback-safe fake-data contract proving invalid inspiration photo uploads return a customer-visible summary and create record-level Lead evidence. | After changing public inquiry upload handling, accepted file types, upload limits, success-modal copy, or record-level failure reporting |
| `payment_success_reconciliation_contract.py` | No-live contract proving browser-return paid checkout shows a reconciliation-pending thank-you state when invoice/receipt/email follow-up is not fully complete. | After changing `/payment-success`, `/thank-you`, paid-order reconciliation, receipt email handling, or payment-success automation checks |
| `customer_contact_points_contract.py` | Read-only inventory contract for customer forms/contact points, CRM/order targets, customer-visible failure behavior, approved customer-facing inboxes (`hi@`, `legal@`, `billing@`), and required internal business-copy routing. | After changing any public form, contact link, checkout/order message, newsletter signup, product/cart handoff, or business-copy policy |
| `cloudflare_launch_readiness.py` | Public HTTP cutover probe for Cloudflare-in-front-of-Frappe. Fails when `/login`, `/me`, `/contact`, `/cart`, `/checkout`, `/payment-success`, `/thank-you`, Frappe API ping, or the Stripe webhook path are missing, challenged, or served from dynamic cache. | After Cloudflare points staging or production traffic at Frappe Cloud, before calling DNS cutover healthy |
| `cloudflare_launch_readiness_contract.py` | Fast no-network contract for Cloudflare dynamic-route cache-status handling. Proves `MISS`, `HIT`, `STALE`, `UPDATING`, `EXPIRED`, and `REVALIDATED` fail while absent headers, `BYPASS`, and `DYNAMIC` pass. | After changing `cloudflare_launch_readiness.py` or Cloudflare cache-status policy |
| `payment_launch_readiness.py` | Non-secret Stripe/Frappe live-readiness verifier. Local mode checks structural payment config; live mode requires explicit HTTPS `host_name`, live Stripe settings, payment gateway account, payment method configuration, operator email, webhook secret, and public policy routes. | Before real Stripe checkout testing or live payment cutover |
| `complex_checkout_scaffold.py` | Read-only local ERPNext ecommerce scaffold report. Refreshes ProductPatternContract data and maps every product into direct-checkout regression guard, simple purchasable proof candidate, multi-color UI required, add-on/conditional blocked, or needs-review stages without authorizing a live update. Current `quote_first` field values are legacy/internal hold states, not the target business model. | Before building complex product checkout UI, moving held products into purchasable checkout, or claiming ERPNext ecommerce scaffolding is ready for staging |
| `complex_checkout_scaffold_contract.py` | Fast pure contract for the complex checkout scaffold classifier. Proves direct checkout guards, simple candidates, multi-color builder requirements, add-on/conditional blockers, and Classic Arch last-stage rules. | After changing `locally_twisted.catalog_contract.complex_checkout_scaffold` or the complex-product proof ladder |
| `product_page_architecture_contract.py` | Read-only source/live contract for the generic backend-driven product-page receiving architecture. Maps ProductPatternContract axes into page controls, versioned cart payload keys, server-derived resolver fields, and Quotation/Sales Order/Sales Invoice line parity without product-specific branches. | Before changing product-page controls, checkout admission, cart payload shape, quote-first handoff, or claiming architecture rather than product-specific readiness |
| `product_page_architecture_contract_contract.py` | Fast pure contract for the product-page receiving architecture builder. Proves sale-unit, color customization, add-on, review-only, payload-target, document-parity, and no-product-specific-rules behavior without Docker. | After changing `locally_twisted.catalog_contract.product_page_architecture_contract` or template architecture emission helpers |
| `product_blueprint_contract.py` | Pure contract checks for the employee-facing `LT Product Blueprint` Desk records. Proves the blueprint schema has no live publish action, maps staff-entered options/add-ons/color recipes into LT payload targets, blocks unsafe checkout, builds a no-write dry-run apply plan, guards local apply behind explicit confirmation, exposes a Desk button through a server-gated method, and keeps live readiness false. | After changing product blueprint DocTypes, product setup validation, add-on/conditional-pricing blueprint rules, dry-run apply planning, guarded local apply, Desk apply controls, or employee product-authoring workflows |
| `product_blueprint_live_contract.py` | Rollback-safe live ERPNext contract for `LT Product Blueprint`. Proves the DocTypes are installed, a quote-first blueprint saves validation and dry-run apply evidence, live approval and blocked preview fail loudly, the Desk preview rejects Guest users, Desk apply blocks without `lt_allow_local_blueprint_apply`, guarded local apply creates only expected local product records in-transaction, `Item Manager` and `System Manager` users can apply locally without raw Website Item profile access, blueprint-authored checkout add-ons appear in product options and checkout line generation, a 48-variant complex media product resolves the selected image through product page/cart/checkout payloads, Website Items stay unpublished, no order/invoice/payment records are created, and rollback restores guard counts. | After migrating product blueprint DocTypes locally and before claiming the staff product-authoring surface works in ERPNext Desk |
| `product_blueprint_release_smoke.py` | Local-only committed helper that creates a durable employee-authored Product Setup proof product with many variants and generated/uploaded media URLs, then publishes the local Website Item for browser/customer-journey proof. This intentionally mutates local ERPNext and is not rollback-safe. | Only for local release proof after `product_blueprint_live_contract.py` passes and before manual/browser checkout testing; never run against staging/live without explicit approval |
| `product_page_architecture_readiness.py` | Read-only ERPNext/Frappe ecommerce-template readiness audit. Maps the active goal to pass/blocker rows for two reusable product-page types, line preservation, quote flow, operator review, BCC-gated customer delivery, dependency helpers, GL-cleared source price/media/add-on business packets, public ecommerce exposure lock, and finance deferral. The JSON separates `technical_architecture_ok` from `import_reopen_ok`; with `lt_ecommerce_paused=0` the current expected result is pass, while the locked public site config keeps `public_ecommerce_reopen` blocked without blocking local build/test work. | Before claiming the ecommerce template architecture is complete, before destructive catalog import/rebuild, or before reopening public shop/cart/checkout |
| `product_import_readiness_gate.py` | Read-only real-catalog import readiness gate for the corrected V1 import path. Checks source packets, V1 manifest, price/media/add-on decisions, purge scope, fresh snapshot, backup guard paths, import-runner field markers, destructive guards, and final approval. | Before any catalog purge/import rehearsal or after changing source import artifacts, `seed_catalog.py`, guard packets, snapshot/backup evidence, or approval packets |
| `product_import_readiness_gate_contract.py` | Fast pure regression for readiness-gate no-snapshot command-packet handling. Proves missing `current-state-snapshot-*` returns a blocker placeholder instead of crashing report construction. | After changing `product_import_readiness_gate.py` snapshot or command-packet helpers |
| `post_import_catalog_state_contract.py` | Fast pure regression for post-import catalog-state evaluation. Proves missing/unpublished/disabled/unpriced included products return `ok: false`, while a fully ready fake included product returns `ok: true`. | After changing `locally_twisted.verify.post_import_catalog_state` or the corrected V1 post-import pass/fail rules |
| `product_page_runtime_contract.py` | Rollback-safe live ERPNext contract for the reusable LT product-page runtime: Website Item page-type fields, plain page-template labels, live fallback page-type inference, Sales Order Item/Sales Invoice Item/Quotation Item configuration storage, Lead product-page quote handoff fields, `LT Product Quote Item` child rows, automatic draft Quotation cascade, draft quote/proposal packet visibility, Quotation Item to Sales Order Item payload copy, versioned cart payloads, checkout line preservation, confirmed foil-number add-on line expansion, add-on option eligibility, invoice copy, quote-first checkout blocking, quote-first payload detail normalization/loud failures, and stale-cart loud failure. | After changing product-page type fields, page-template labels, Webshop item templates, guest cart schema, checkout line creation, invoice creation, quote-first controls, contact product handoff, Lead cascade quote behavior, draft Quotation helpers, quote acceptance preservation, quote/proposal packet review fields, add-on runtime helpers, add-on selector helpers, or product-page runtime helpers |
| `product_add_on_dependency_contract.py` | Live no-mutation contract for product-page add-on boundaries. Proves confirmed `foil_number` stays a priced checkout add-on, while known unapproved source families (`Add ons`, `Plush add ons`, `Orbz toppers`, `Add Bouquet`) route to quote before paid checkout. | After changing add-on source classification, runtime add-on contracts, checkout add-on validation, add-on dependency rules, or source-import add-on blockers |
| `product_add_on_approval_packet.py` | Read-only source-backed approval packet for unapproved add-on families. Writes `22-product-add-on-approval-packet.*`, listing affected products, source values, decision needed, and the enforced `quote_only_until_approved` default for `Add ons`, `Plush add ons`, `Orbz toppers`, and `Add Bouquet`. It must never approve checkout by itself. | Before asking GL/Jeff to approve add-on classes, before building another add-on selector, and after changing review-only add-on source rules |
| `product_page_contract_source_audit.py` | Read-only source-catalog dry-run for the two reusable product-page template types. Reports Ready-to-order page vs Custom quote page classification, commerce lane labels, confirmed add-ons, resolver-price blockers, gallery/media blockers, color customization blockers, review-only axes, and JSON review-only add-on family counts without mutating ERPNext. Exit code `2` is the expected blocked-import state until source enrichment is complete. | Before destructive catalog purge/import, after changing source contract models/builders, or before claiming product concepts are mappable into the ERPNext template architecture |
| `product_page_dependency_contract.py` | Read-only source-catalog contract for option dependency matrices. Proves source `valid_variants` collapse into reusable product-page required-axis combinations without treating color customization or add-on axes as required SKU dependencies, and proves the pure availability helper narrows partial selections while failing loudly on impossible or unknown axes. | After changing source contract models/builders, required-axis classification, color/add-on axis removal, dependency availability helpers, or product-page option availability logic |
| `product_page_price_readiness_contract.py` | Read-only live ERPNext price-readiness gate for product-page checkout. Compares source contract sale units, current backend Website Item page/lane fields, and active live Item/Item Price rows; the checkout price gate follows the backend `lt_commerce_lane`, while source-template differences remain visible as audit context. It also distinguishes current live checkout price coverage from still-missing source `erpnext_variant_price` reimport evidence and writes `audits/odoo-erpnext-migration-audit-2026-05-08/19-product-page-price-readiness-report.md`. | Before reopening ready-to-order ecommerce, after changing product classification, source variant projection, Website Item buying path, Item Price repair, or server pricing rules |
| `product_page_price_enrichment_contract.py` | Read-only source import price-candidate gate for the two product-page template types. Writes a human report and machine-readable row-level candidate artifact under `audits/odoo-erpnext-migration-audit-2026-05-08/21-*`, preserving every source variant row, dropped axis, projected sale-unit key, live match state, and price source. It proves every expected import sale unit has a candidate from source resolver rows, source base price, or the current live ERPNext snapshot while marking live-snapshot candidates as business-review required and keeping purge/reimport blocked when non-price gates remain. | Before any purge/reimport rehearsal, after changing source variant projection, live Item Price repair, or import-price provenance rules |
| `product_page_price_review_packet.py` | Read-only business price review packet extracted from the price-enrichment artifact. Writes `24-product-page-price-review-packet.*`, listing every live ERPNext snapshot-priced sale unit that still needs approval or replacement, and keeps approved public price count at zero. | Before treating live-snapshot candidates as customer-facing prices, before destructive reimport, and after changing price provenance/review rules |
| `product_source_repair_map.py` | Read-only source-backed map for the 53 existing products. Refreshes the complex checkout scaffold, joins it to the Odoo export and price-enrichment artifact, writes `workstreams/ecommerce-audit/product-source-repair-map-2026-05-17.*`, and translates legacy held Website Item states into purchasable repair lanes instead of business quote-first categories. | Before choosing the next product certification tranche, after repulling the Odoo product export, after changing scaffold stages, or before claiming unresolved products are intentionally non-purchasable |
| `product_source_repair_map_contract.py` | Fast pure regression for the source repair map. Proves a legacy held field becomes `purchasable_product` / `blocked_until_certified`, that business repair fields do not contain quote terminology, and that missing Odoo source rows fail loudly. | After changing `locally_twisted.catalog_contract.product_source_repair_map`, product repair lane names, or source/scaffold joining logic |
| `simple_purchasable_rehearsal_contract.py` | Rollback-safe live ERPNext contract for the first simple blocked-product tranche. Temporarily applies `simple_product|checkout` inside one transaction for `large-head-missionary`, `mothers-day-front-yard-7-column`, `easter-arch`, and `pride-arch`, then proves 33 sale SKUs preserve source-backed prices and SO/SI line configuration fields before rollback. | Before promoting those four held products, after changing their prices, variant axes, Website Item contracts, or line preservation behavior |
| `simple_purchasable_browser_proof.py` | Local-only browser proof for the same four simple repair-lane products. Temporarily opens local ecommerce and applies checkout contracts, runs `post_import_checkout_proof.js` with a custom product list, then restores original Website Item fields and `lt_ecommerce_paused`. | Before promoting those four held products to customer checkout, after changing their templates, routes, selector controls, cart behavior, or checkout preview behavior |
| `product_page_media_visibility_contract.py` | Read-only media visibility gate for the two reusable product-page template types. Reports source primary/extra images, unclassified source extras, live Website Item images, Website Slideshow presence, active variant image coverage, and public-ecommerce-pause caveats without assigning or moving images. Exit code `2` is the expected blocked-import state until source media is classified and approved. | Before assigning gallery/variant/category media, before claiming product-page media is ready, after changing source media classification, variant media sync, product gallery rendering, or ecommerce pause assumptions |
| `product_page_media_classification_packet.py` | Read-only source-backed classification packet for source extra product-page images. Writes `23-product-page-media-classification-packet.*`, listing each product/image row, allowed classification roles, and the enforced `hold_until_classified` default while keeping approved gallery and variant assignments at zero. | Before assigning product gallery/variant/category media, before asking GL/Jeff to classify product images, and after changing media classification rules |
| `variant_media_contract.py` | Live variant-media behavior gate for the current temporary ERPNext variant image bridge. Verifies the API returns a variant image, guest product routes stay paused, then logs in as an operator and proves exact option selection swaps the main image on the product page. | After changing `api/variant_media.py`, variant image sync, product image template behavior, ecommerce pause behavior, or product option selectors |
| `checkout_product_family_contract.py` | Rollback-safe live ERPNext contract for current purchasable product-family preservation. Proves 18 backend checkout families, 151 sale SKUs, foil-number add-ons, color-recipe lines, Sales Order rows, Sales Invoice rows, and rollback cleanup without opening public ecommerce or creating live payment sessions. | After changing purchasable product classifications, variant axes, color recipe handling, add-on handling, checkout line generation, Sales Order/Sales Invoice line fields, or product-family cascade proof |
| `cart_checkout_contract.py` | Live cart/checkout contract for guest cart item resolution, configured cart line identity, visible add-on display rows, add-on eligibility rejection, review-only source add-ons blocking paid checkout, color-recipe-to-resolved-variant matching, checkout cart parsing, paused-shop expectations, and quantity caps. | After changing `api/cart.py`, `lt-guest-cart.js`, cart/checkout templates, ecommerce pause behavior, product add-ons, add-on eligibility rules, color recipe payloads, variant resolution, or cart payload shape |
| `checkout_experience.spec.js` | Playwright checkout/cart public experience gate. While ecommerce is paused, it verifies `/cart` and `/checkout` redirect to the branded pause page without exposing checkout controls. | After changing ecommerce pause behavior, cart/checkout pages, or public route gating |
| `product_quote_first_experience.spec.js` | Legacy-named Playwright browser gate for authenticated/operator reusable product-page controls at desktop and mobile widths. Proves internally held products do not fall into ready-to-order cart controls and still carry selected options/design notes into the `/contact` product inquiry payload, while Unicorn Bouquet keeps ready-to-order variant controls plus eligible add-ons. Run with `npm run test:product-quote-first`. | After changing held-product templates, ready-to-order product controls, runtime page-type inference, `/contact` product handoff, or ecommerce pause/operator product-route behavior |
| `post_import_checkout_proof.js` | Direct Node/Playwright browser proof for the corrected V1 post-import checkout path. In open-commerce local mode, adds all 18 current checkout product families at desktop and mobile widths, proves visible color-drawer selections create `color_recipes`, then calls the non-mutating checkout totals preview API before accepting cart/checkout summary proof. Restore `lt_ecommerce_paused=1` after local proof unless the task explicitly opens ecommerce. | After corrected V1 import/reimport work, product selector changes, color-drawer behavior, guest cart schema, ecommerce pause/open-mode testing, or checkout preview validation |
| `business_automation_index.py` | Read-only cross-system automation map for intake, CRM, checkout, payment, paperwork, finance, and scheduled checkups, including the sanitized maintenance heartbeat. Fails nonzero when launch-required surfaces are missing or disconnected. Full verifier runs execute runtime fake-data contracts; internal digest/report callers use non-runtime mode. | Before adding backend automation, before Frappe Cloud readiness claims, and when reviewing cascading information paths |
| `synthetic_business_pipeline.py` | Synthetic no-live pipeline audit for fake-data and rollback-safe backend flows. It runs/currently indexes record-level failure evidence, inquiry upload failure evidence, product add-on dependency boundaries, Stripe amount parity, checkout-to-Lead conversion, checkout fulfillment, paid-order cascade, payment-success reconciliation pending state, mocked Stripe webhook behavior, customer document policy, customer email policy boundaries, outbound document templates, outbound document send-readiness, quote/proposal packet outliers, product quote operator-review outliers, product quote operator-send control, unpaid invoice packet outliers, customer reminder dry-run outliers, and customer reminder review-report outliers while keeping live cutover checks deferred. | When flushing out cascading data, broken piping, fake-data cleanup, and no-live operating readiness |
| `stripe_amount_parity_contract.py` | Contract test proving Stripe Checkout line items equal the ERPNext Sales Order grand total, including tax/charges adjustment and loud rejection when item lines exceed the expected total. | After editing Stripe checkout, Sales Order totals, taxes/charges, delivery fees, or payment request creation |
| `invoice_branding_contract.py` | Verifies the branded Sales Invoice Print Format, default Sales Invoice print-format property, Letter Head, logo asset, and rendered invoice HTML against an existing Sales Invoice. | After editing `sync_invoice_branding.py`, invoice copy/styling, letterhead, or invoice setup records |
| `outbound_documents_contract.py` | Verifies the answer-first standard outbound document registry and source templates under `locally_twisted/outbound_documents/`. | After adding or changing invoices, receipts, proposals, packets, statements, reminders, work orders, or other files sent outside the company |
| `outbound_document_send_readiness_contract.py` | Fake-data contract proving external documents block missing fields, recipient confirmation, required internal copy recipients, payment path, branding, human approval, sensitive attachments, and record-level blocker evidence before any customer delivery. | Before building or changing any sender, reminder Desk page, proposal generator, W-9 packet path, or Frappe Cloud paperwork trust claim |
| `render_outbound_document_previews.py` | Renders fake-data normal and outlier previews for every outbound document template to ignored `output/playwright/outbound-documents-YYYYMMDD/` as HTML, PDF, and PNG, with `Key fields to review` in the high-visibility slot. | When reviewing invoice, receipt, proposal, packet, statement, reminder, work order, contract summary, or follow-up design before automation |
| `inquiry_spam_gate.py` | Live public-form anti-bot gate. Proves `/contact` renders the signed `lt_form_token` and invisible `website` honeypot, and that missing/too-fast/honeypot submissions fail before Lead creation. | After changing public inquiry form markup, submit token handling, anti-spam behavior, or route embedding |
| `inquiry_sales_solicitation_filter.py` | Live public-form sales-suppression gate. Proves a Nicole/vettedvas-style vendor pitch saves reviewable Lead evidence while suppressing owner notification, and that a plausible customer event inquiry still queues the owner email. | After changing `inquiry_sales_filter.py`, owner notification suppression, customer/owner email proof, or spam false-positive boundaries |
| `smoke_forms.py` | Browser smoke test for public forms. Use `--form-path /contact --skip-newsletter` for the current canonical inquiry form. Local localhost runs verify the Lead through the Docker/Frappe bench container and delete the generated smoke Lead plus linked LT cascade Task; non-local authenticated runs can use `LT_ADMIN_PASSWORD`. | Before claiming form submissions work end-to-end |
| `category_media_candidates.py` | Builds a no-mutation JSON/Markdown approval packet for empty customer-facing Item Group images from existing Odoo product-source and portfolio-proof media. Outputs to ignored `output/category-media-candidates.*`. | When preparing category browse imagery for Jeff/GL review before assigning live Item Group images |
| `playwright_home_screenshot.py` | Real-Chromium full-page screenshot capture at desktop + mobile viewports + DOM facts dump. | Before declaring a visible change done |

## Standing Rules

- Layout fit is necessary but not sufficient. `npm run test:layout-fit` catches geometry regressions; it does not replace screenshot review or GL's real-browser check.
- Axe is part of public-site closeout for route/template changes. Accessibility launch blockers include contrast, landmarks, link names, heading order, breadcrumb regions, and keyboard/screen-reader semantics — not only visible styling.
- Idempotency over magic. Every script in `setup/` and `dev/` should check-then-act or no-op cleanly when state already exists.
- Loud errors. If a script's API call returns an error, surface it.
- No `deploy.py` yet. Frappe Cloud cutover is Phase 6. Until then, deployment-like operations live as discrete scripts in `setup/` or `dev/`.
