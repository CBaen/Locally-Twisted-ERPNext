#!/usr/bin/env bash
set -euo pipefail

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
cli="$repo_root/node_modules/@playwright/test/cli.js"

if [[ ! -f "$cli" ]]; then
  echo "Playwright test CLI not found at $cli" >&2
  exit 1
fi

exec node "$cli" "$@"
