# Tighten Complete — Contestant 2

Three proxy items applied. Ghost mechanic untouched.

**1. Visual thumbnails inside Design pills** (`02-color-one.html`). Replaced text-only pills with flex-column buttons containing inline SVG pattern previews: Swirl shows A-B-A-B alternating circles (Blush / Dusk Blue), Layered shows A-A-B-B banded circles, Organic shows four distinct colors in no fixed pattern. Pills are now `border-radius: 12px` to accommodate the two-row layout. Active-state CSS inverts thumbnail circle fills to white-on-teal so the pattern reads on the dark pill background. Labels updated to "alternating colors" / "color in bands" / "mixed & flowing". This borrows C4's thumbnail move directly and removes the vocabulary gate before the ghost mechanic can do its work.

**2. "Permission to ignore" audit** (all screens). Full end-to-end pass: `01-entry.html` uses "no experience needed" (correct register); `02-color-one.html` has no completion language; `04-composition.html` scene label says "Your design is ready — tap to edit, or send it as-is" (completion named first, ghost is bonus); ghost label reads "Add garland? / totally optional"; `06-upsell.html` header says "Want to add more? totally optional", skip button says "No thanks, I'm done", ghost tap label says "+ Add one? optional"; `05-done.html` says "Sending is free. No commitment." No residual obligation language found. Flow is consistent: the customer is told they're done before they ever see a ghost suggestion.

**3. Done-screen portrait card** (`05-done.html`). Canvas SVGs updated to real LT palette (Blush #F4C2C2 / Dusk Blue #7FA3C0 / White — replacing the generic #FFB6C1 / #87CEEB that drifted in from Round 1). Centerpiece removed from both canvas and Pieces row. Color section restructured with region labels (Main / Accent / Crown) preceding each swatch + name + hex, giving Jeff the per-region attribution he needs for a supplier call. Pieces row updated to "Balloon Arch · Swirl" and "Column · Swirl" — design style visible inline, no centerpiece.

**Ghost mechanic: untouched.** Pre-tinted color inheritance and `initGhostInheritance()` are unchanged per proxy instruction. The cascade is the crown jewel.
