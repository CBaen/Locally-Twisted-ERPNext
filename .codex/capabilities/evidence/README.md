# Evidence

Store compact LT capability evidence here when it exists.

Do not store secrets, client records, raw private conversations, or long logs.
Use one short JSON object per line in `capability-evidence.jsonl`.
