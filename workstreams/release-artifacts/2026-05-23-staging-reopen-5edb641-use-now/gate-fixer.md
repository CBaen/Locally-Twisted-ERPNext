# Gate/Fixer

Role: identify the exact gate that needs to move next and prevent shortcuts.

Target: release gate after completed `app_mirror_sync`.

Evidence: local release-prevention tests, generated packet artifacts,
`app-mirror-sync-plan.json`, controller output, post-sync mirror freshness, and
post-sync provider snapshot.

Good:

- local release-prevention suite passes at the clean source;
- read receipts are now generated by a helper instead of hand-copied;
- the dual-account model is documented and represented in
  `release-identity-proof.json`;
- provider snapshot confirms staging is stable enough for the next controlled
  action.
- app-root mirror freshness now passes at
  `5dd674c5ae9d6b3cb125ecf7ba2dd2e4e65e3831`.

Bad:

- staging installed app is still at
  `181076c239b2d1d3d508a41ac471c71f9d2b5158`;
- staging still cannot be owner-review ready until mirror sync, deploy/update,
  hosted preflight, bootstrap/import, and owner-review verification all pass;
- the current `freeze-reopen-approval.json` authorizes only
  `app_mirror_sync`, not deploy/update.

Next gate:
fresh bounded approval and controller evaluation for `frappe_cloud_deploy`
/ deploy-update only.

Current result: **PASS** for app mirror sync completion; **NO-GO** for
deploy/update/bootstrap/owner-review readiness until the next approval exists.

Deploy/update gate result:

- Attempt 1 exposed a missing guard: `sites` must be the complete current
  Frappe Cloud site object, not only the site name.
- Corrected payload validation passed.
- The release controller passed before attempt 2.
- Attempt 2 completed provider deploy/update proof.

Next gate:
hosted preflight only after fresh approval, then bootstrap/import only if the
hosted preflight passes and GL explicitly reopens that action.

Hosted preflight result:

- `target_hash` passed: staging reports
  `5dd674c5ae9d6b3cb125ecf7ba2dd2e4e65e3831`.
- `app_order`, `app_hooks`, and `standard_report` passed.
- Blockers:
  - missing Role `LT Marketing Review Access`;
  - `Webshop Settings.enable_checkout=0`, expected `1`;
  - destructive catalog seed requires real `backup_artifact` or explicit
    `zero_data_proof`.

Current result: **NO-GO** for bootstrap/import. Do not bypass this with a
manual seed, cache clear, migrate, or owner-review claim.
