# Provider Witness Artifact

target: locallytwisted-staging.frappe.cloud
state: NO-GO READ-ONLY SNAPSHOT ONLY
evidence: `provider-snapshot.json` must be generated by `scripts/verify/frappe_cloud_provider_snapshot.py` in read-only mode or replaced by a stricter provider witness packet before any mutation.

## Boundary

Provider witness work may inspect current staging/provider state. It may not
deploy, update, bootstrap, migrate, cache clear, poll a running mutation, touch
live, DNS, Stripe, Search Console, production indexing, or checkout exposure.
